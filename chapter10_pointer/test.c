// #include<stdio.h>
// int main(){
//     int * prt;
//     int fort[2][2] = {{12},{14,16}};
//     prt = fort[0];
//     printf("/* prt is %d/n,/*(prt+2) is %d/n",*prt,*(prt+4));
//     prinft("p")
//     return 0;
// }
#include <stdio.h>

int main(void) {
    // 定义和初始化数组
    // Define and initialize the array
    float rootbeer[10], things[10][5], *pf, value = 2.2;
    int i = 3;
    printf("the address of rootbeer is %p\n", &rootbeer);
    scanf("%f", &rootbeer[0]);

    return 0;
}